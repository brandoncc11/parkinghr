<%= form_with(model: tariff, local: true) do |form| %>
  <% if tariff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tariff.errors.count, "error") %> prohibited this tariff from being saved:</h2>

      <ul>
      <% tariff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :tariff_name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.number_field :value, id: :tariff_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
